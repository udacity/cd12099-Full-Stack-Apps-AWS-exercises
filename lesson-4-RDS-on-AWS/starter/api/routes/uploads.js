const express = require("express");
const router = express.Router();
const models = require("../models");
math = require("mathjs");



//TODO Create new endpoint to save data to the database

module.exports = router;